# Kubernetes (K8s) 技术栈介绍与虚拟机部署对比

## 1. Kubernetes 是什么？

Kubernetes（简称 K8s）是一个开源的容器编排引擎，用于自动化容器化应用程序的**部署**、**扩展**和**管理**。它最初由 Google 设计，深受 Google 内部 Borg 系统的影响。

## 2. Kubernetes 核心架构与技术栈

K8s 采用 **Master-Worker** 架构，主要由控制平面（Control Plane）和工作节点（Worker Node）组成。

### 2.1 控制平面 (Control Plane / Master Node)
控制平面负责集群的全局决策（如调度）和检测/响应集群事件。

*   **API Server (`kube-apiserver`)**: 集群的统一入口，处理 REST 请求，验证并配置数据（只有它直接与 etcd 交互）。
*   **etcd**: 分布式键值存储系统，保存集群的所有配置数据和状态信息（这是 K8s 的数据库）。
*   **Scheduler (`kube-scheduler`)**: 负责监视新创建的 Pod，并根据资源需求、策略约束等将其**调度**到合适的 Worker Node 上。
*   **Controller Manager (`kube-controller-manager`)**: 运行各种控制器进程（如节点控制器、副本控制器），负责维护集群的状态，确保**期望状态**与**实际状态**一致。
*   **Cloud Controller Manager**: 与底层云提供商（如 AWS, GCP, Alibaba Cloud）交互的组件（用于管理负载均衡器、存储卷等）。

### 2.2 工作节点 (Worker Node)
工作节点是运行应用程序容器的主机。

*   **Kubelet**: 运行在每个节点上的代理，负责维护容器的生命周期（拉取镜像、启动容器），并向 Master 汇报节点状态。
*   **Kube-proxy**: 维护节点上的网络规则，实现了 Service 的负载均衡（流量转发）。
*   **Container Runtime**: 负责真正运行容器的软件。
    > **注意**: 虽然 Docker 是最著名的容器工具，但 K8s 从 v1.24 版本开始移除了对 **Docker Engine** (Dockershim) 的直接支持。
    > *   **生产环境主流**: 现在主要使用 **containerd** (Docker 的底层运行时) 或 **CRI-O**。
    > *   **开发者体验**: 你仍然可以使用 Docker 构建镜像，K8s 依然完美支持运行 Docker 格式的镜像（OCI 标准）。

### 2.3 核心资源对象 (Objects)
*   **Pod**: K8s 中最小的部署单元。一个 Pod 可以包含一个或多个紧密耦合的容器（共享网络 IP 和存储）。
*   **Service**: 定义一组 Pod 的逻辑集合和访问策略（负载均衡），提供稳定的 IP 地址（VIP）。
*   **Deployment**: 用于管理无状态应用，提供声明式的更新能力（如滚动更新、回滚）。
*   **StatefulSet**: 用于管理有状态应用（如数据库），保证部署和缩放的顺序性、唯一性。
*   **ConfigMap / Secret**: 用于配置管理和敏感信息存储，解耦配置与镜像。
*   **Ingress**: 管理外部访问集群服务的规则（HTTP/HTTPS 路由）。

---

## 3. Kubernetes vs. 传统虚拟机 (VM) 部署

### 3.1 部署模式演进
1.  **传统部署**: 直接在物理机上运行应用。资源分配不均，难以隔离。
2.  **虚拟化部署 (VM)**: 在物理机上通过 Hypervisor 运行多个虚拟机（VM）。每个 VM 都有完整的 OS。隔离性好，但资源开销大。
3.  **容器化部署 (K8s)**: 容器共享宿主机的 OS 内核，比 VM 更轻量。K8s 对容器进行统一编排。

### 3.2 详细对比

| 特性 | 传统虚拟机 (Virtual Machine) | Kubernetes / 容器 (Container) |
| :--- | :--- | :--- |
| **架构层次** | 硬件 -> Hypervisor -> Guest OS -> App | 硬件 -> Host OS -> Container Engine -> App |
| **隔离级别** | **硬件级隔离** (强)。每个 VM 都有独立的内核。 | **进程级隔离** (较弱)。共享宿主机内核，通过 Namespace 和 Cgroups 隔离。 |
| **资源占用** | **重**。每个 VM 需要运行完整的 OS，内存/磁盘占用大。 | **轻**。只包含应用及其依赖库，启动快，资源利用率高。 |
| **启动速度** | 分钟级 (需启动 OS)。 | **秒级** (类似启动进程)。 |
| **可移植性** | 较差。镜像庞大，依赖特定 Hypervisor。 | **极好**。一次构建，到处运行 (Build once, Run anywhere)。 |
| **扩展性** | 较慢。扩容需要复制大镜像并启动 OS。 | **极快**。秒级启动新 Pod，K8s 自动水平扩展 (HPA)。 |
| **管理复杂度** | 相对简单，像管理物理机一样。 | **高**。特别是 K8s 本身学习曲线陡峭，网络和存储配置复杂。 |
| **DevOps** | 环境一致性难保证（"它在我机器上能跑"）。 | **Immutable Infrastructure** (不可变基础设施)，开发/测试/生产环境高度一致。 |

### 3.3 优缺点总结

#### 只使用 VM 的优缺点
*   **优点**: 强隔离安全性高；技术成熟；适合运行不同内核 OS 的应用。
*   **缺点**: 资源浪费严重（每个 Guest OS 都有开销）；启动慢；环境漂移问题。

#### 使用 K8s 的优缺点
*   **优点**:
    *   **资源利用率极致**: 可以在同一台机器上高密度部署。
    *   **自愈能力**: 进程崩溃自动重启，节点宕机自动调度迁移。
    *   **自动化运维**: 自动扩缩容，自动发布/回滚。
    *   **云原生标准**: 事实上的行业标准，生态丰富。
*   **缺点**:
    *   **学习成本高**: 概念多，运维复杂。
    *   **安全性**: 共享内核，如果存在内核逃逸漏洞会影响宿主机（可通过安全容器如 Kata Containers 解决）。

## 4. 总结建议

*   如果是**遗留的单体应用**，或者对**安全性/强隔离**有极端要求（如多租户核心数据隔离），**虚拟机**可能更合适。
*   如果是**微服务架构**、需要**快速迭代**、**高弹性**伸缩的现代应用，**Kubernetes** 是不二之选。
